package at.george;

public interface Writer {

    /**
     * Writes the count to an User Interface
     *
     * @param count The count to be written to an User Interface, must not be negative
     */
    void write(long count);
}
